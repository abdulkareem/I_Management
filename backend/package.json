{
  "name": "@internsuite/backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "echo 'Backend build completed'",
    "prisma:bootstrap": "node ./scripts/prisma-bootstrap.mjs",
    "start": "npm run prisma:bootstrap && wrangler dev --remote"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "wrangler": "^4.8.0"
  }
}
