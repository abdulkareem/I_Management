{
  "name": "internsuite-platform",
  "private": true,
  "version": "2.0.0",
  "workspaces": [
    "frontend",
    "backend",
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  {
  "scripts": {
    "build": "npx prisma generate --schema=backend/prisma/schema.prisma && npm run build"
  }
}
  {
  "prisma": {
    "schema": "backend/prisma/schema.prisma"
  }
}
}
