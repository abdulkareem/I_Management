{
  "name": "internsuite-platform",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "dev": "sh -c 'npm run dev --prefix backend & npm run dev --prefix frontend'",
    "start": "npm run start --prefix backend",
    "lint": "npm run lint --prefix backend && npm run lint --prefix frontend",
    "typecheck": "npm run typecheck --prefix backend && npm run typecheck --prefix frontend",
    "test": "npm run test --prefix backend",
    "frontend:build": "npm run build --prefix frontend",
    "backend:build": "npm run build --prefix backend",
    "prisma:generate": "npm run prisma:generate --prefix backend",
    "prisma:migrate:deploy": "npm run prisma:migrate:deploy --prefix backend",
    "pages:build": "npm install --prefix frontend --progress=false && npm run build --prefix frontend"
  }
}
