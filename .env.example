# Web (Cloudflare Pages)
NEXT_PUBLIC_API_BASE_URL=https://internsuite-api.<your-subdomain>.workers.dev

# API (Cloudflare Worker)
JWT_SECRET=replace-with-long-random-value
OTP_SECRET=replace-with-different-long-random-value
