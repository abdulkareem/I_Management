{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm prisma:generate && pnpm --filter @prism/api build"
  },
  "deploy": {
    "startCommand": "pnpm prisma:migrate:deploy && pnpm --filter @prism/api start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
